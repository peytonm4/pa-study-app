---
phase: 04-content-generation
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - src/Api.Tests/Generation/SectionGenerationJobTests.cs
  - src/Api.Tests/Generation/AlgorithmicDetectionTests.cs
  - src/Api.Tests/Generation/StubGenerationProviderTests.cs
  - src/Api.Tests/Generation/GenerationTriggerTests.cs
autonomous: true
requirements: [GEN-01, GEN-02, GEN-03, GEN-04, GEN-05, GEN-06, GEN-07, GEN-08]

must_haves:
  truths:
    - "Full test suite compiles and passes baseline (55 tests, same 2 pre-existing failures)"
    - "Four new test class files exist in src/Api.Tests/Generation/ with skipped stubs"
    - "No new types referenced that do not yet exist — stubs use only xUnit"
  artifacts:
    - path: "src/Api.Tests/Generation/SectionGenerationJobTests.cs"
      provides: "Skipped stubs for GEN-01 through GEN-07"
    - path: "src/Api.Tests/Generation/AlgorithmicDetectionTests.cs"
      provides: "Skipped stubs for GEN-07 keyword detection"
    - path: "src/Api.Tests/Generation/StubGenerationProviderTests.cs"
      provides: "Skipped stubs for GEN-08 stub correctness"
    - path: "src/Api.Tests/Generation/GenerationTriggerTests.cs"
      provides: "Skipped stubs for POST /modules/{id}/generate integration tests"
  key_links:
    - from: "src/Api.Tests/Generation/*.cs"
      to: "xUnit only (no production type references)"
      via: "using Xunit;"
      pattern: "Fact\\(Skip"
---

<objective>
Create Wave 0 test scaffolds: four test class files in the new Generation/ directory with skipped
[Fact(Skip=...)] stubs. These establish the test contract before any production code exists so all
plans can reference them as verification targets.

Purpose: Nyquist compliance — every subsequent plan needs a test to run against. Wave 0 produces
compilable stubs so `dotnet test --filter "Generation"` runs (and skips) immediately.

Output: Four .cs files in src/Api.Tests/Generation/ using only xUnit — no references to
production types that don't exist yet.
</objective>

<execution_context>
@/Users/Peyton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Peyton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-generation/04-CONTEXT.md
@.planning/phases/04-content-generation/04-VALIDATION.md

<interfaces>
<!-- Wave 0 uses ONLY xUnit — no production type imports. No references to GenerationRun,
     SectionGenerationJob, ContentGenerationJob, or StubGenerationProvider (none exist yet).
     Stubs must compile clean against the existing codebase. -->

Existing test infrastructure pattern (from src/Api.Tests/Extraction/LectureExtractionJobTests.cs):
- Namespace: StudyApp.Api.Tests.{Folder}
- In-memory DB: new DbContextOptionsBuilder<AppDbContext>().UseInMemoryDatabase(...)
- All [Fact] tests that reference non-existent types must be [Fact(Skip="Wave 0 stub")]

Wave 0 rule: test files may NOT import any type that doesn't already exist in the codebase.
Use only: using Xunit; and a class with [Fact(Skip="Wave 0 stub")] methods.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Generation test directory with SectionGenerationJobTests and AlgorithmicDetectionTests stubs</name>
  <files>
    src/Api.Tests/Generation/SectionGenerationJobTests.cs
    src/Api.Tests/Generation/AlgorithmicDetectionTests.cs
  </files>
  <action>
Create directory src/Api.Tests/Generation/ and write two files.

SectionGenerationJobTests.cs — namespace StudyApp.Api.Tests.Generation, using Xunit only. One class
`SectionGenerationJobTests` with these 6 stubs (all [Fact(Skip="Wave 0 stub")]):
- Execute_CreatesStudyGuide_ForEverySection
- Execute_CreatesFlashcards_ForEverySection
- Execute_CreatesQuizQuestions_ForEverySection
- Execute_AlgorithmicSection_CreatesConceptMap
- Execute_NonAlgorithmicSection_NoConceptMap
- Execute_SourceRefs_PresentOnGeneratedContent

AlgorithmicDetectionTests.cs — namespace StudyApp.Api.Tests.Generation, using Xunit only. One class
`AlgorithmicDetectionTests` with these 3 stubs (all [Fact(Skip="Wave 0 stub")]):
- IsAlgorithmic_AlgorithmKeyword_ReturnsTrue
- IsAlgorithmic_WorkupKeyword_ReturnsTrue
- IsAlgorithmic_PlainSection_ReturnsFalse
  </action>
  <verify>
    <automated>dotnet test /Users/Peyton/repos/study-webapp-pa-school/src/Api.Tests/StudyApp.Api.Tests.csproj --filter "Generation" --nologo 2>&amp;1 | tail -5</automated>
  </verify>
  <done>Both files compile. dotnet test --filter "Generation" runs without build errors. The 6+3 stubs
appear as Skipped in output.</done>
</task>

<task type="auto">
  <name>Task 2: Create StubGenerationProviderTests and GenerationTriggerTests stubs</name>
  <files>
    src/Api.Tests/Generation/StubGenerationProviderTests.cs
    src/Api.Tests/Generation/GenerationTriggerTests.cs
  </files>
  <action>
StubGenerationProviderTests.cs — namespace StudyApp.Api.Tests.Generation, using Xunit only. One
class `StubGenerationProviderTests` with these 4 stubs (all [Fact(Skip="Wave 0 stub")]):
- GenerateAsync_StudyGuidePrompt_ReturnsValidJson
- GenerateAsync_FlashcardPrompt_ReturnsValidJson
- GenerateAsync_QuizPrompt_ReturnsValidJson
- GenerateAsync_ConceptMapPrompt_ReturnsValidJson

GenerationTriggerTests.cs — namespace StudyApp.Api.Tests.Generation, using Xunit only. One class
`GenerationTriggerTests` with these 4 stubs (all [Fact(Skip="Wave 0 stub")]):
- PostGenerate_WhenExtractionReady_Returns202AndEnqueuesJob
- PostGenerate_WhenExtractionNotReady_Returns409
- PostGenerate_WhenGenerationQueued_Returns409
- PostGenerate_CreatesGenerationRunInDb

After writing, run the full suite to confirm the pre-existing baseline is preserved:
`dotnet test /Users/Peyton/repos/study-webapp-pa-school/src/Api.Tests/StudyApp.Api.Tests.csproj --nologo`
Expect: same 52 passed / 2 failed / 1 skipped, plus the 13 new skipped stubs.
  </action>
  <verify>
    <automated>dotnet test /Users/Peyton/repos/study-webapp-pa-school/src/Api.Tests/StudyApp.Api.Tests.csproj --nologo 2>&amp;1 | tail -8</automated>
  </verify>
  <done>All 4 files compile. Full suite passes baseline (52 pass, 2 fail pre-existing, 14 skipped total
including the 13 new stubs). No regressions.</done>
</task>

</tasks>

<verification>
dotnet test /Users/Peyton/repos/study-webapp-pa-school/src/Api.Tests/StudyApp.Api.Tests.csproj --nologo
Expected: 52 passed, 2 failed (pre-existing), 14+ skipped (including 13 new Wave 0 stubs)
</verification>

<success_criteria>
- src/Api.Tests/Generation/ directory exists with 4 .cs files
- All 4 files compile with no production type imports
- dotnet test --filter "Generation" runs (all skip, zero errors)
- Pre-existing 52 pass / 2 fail baseline unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-generation/04-01-SUMMARY.md`
</output>
