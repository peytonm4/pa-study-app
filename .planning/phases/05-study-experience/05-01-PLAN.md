---
phase: 05-study-experience
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/Api/Models/SectionProgress.cs
  - src/Api/Data/AppDbContext.cs
  - src/Api.Tests/Study/StudyEndpointTests.cs
  - src/Api.Tests/Study/StudyProgressTests.cs
autonomous: true
requirements: [STUDY-01, STUDY-02, STUDY-03, STUDY-05]
must_haves:
  truths:
    - "SectionProgress entity exists with SectionId, UserId, CardsReviewed, QuizCompleted fields"
    - "EF migration AddSectionProgress exists and applies cleanly"
    - "Test stubs compile and show as skipped (not failed) in test runner"
    - "mermaid package installed in Frontend"
    - "tabs component generated by shadcn in src/Frontend/src/components/ui/tabs.tsx"
  artifacts:
    - path: "src/Api/Models/SectionProgress.cs"
      provides: "SectionProgress EF entity"
    - path: "src/Api/Data/AppDbContext.cs"
      provides: "DbSet<SectionProgress> and OnModelCreating config"
    - path: "src/Api.Tests/Study/StudyEndpointTests.cs"
      provides: "Skipped stub facts for STUDY-01/02/03"
    - path: "src/Api.Tests/Study/StudyProgressTests.cs"
      provides: "Skipped stub facts for STUDY-05"
    - path: "src/Frontend/src/components/ui/tabs.tsx"
      provides: "shadcn Tabs component wrapping @base-ui/react/tabs"
  key_links:
    - from: "src/Api/Models/SectionProgress.cs"
      to: "src/Api/Data/AppDbContext.cs"
      via: "DbSet + OnModelCreating"
      pattern: "SectionProgress"
    - from: "src/Api/Data/AppDbContext.cs"
      to: "EF migration"
      via: "dotnet ef migrations add"
      pattern: "AddSectionProgress"
---

<objective>
Wave 0 setup: create SectionProgress entity and migration, install frontend dependencies (mermaid + shadcn tabs), and plant skipped test stubs so the test suite compiles from the start.

Purpose: All downstream plans build against stable contracts. Tests compile before any implementation exists, preventing surprise build failures mid-wave.
Output: SectionProgress migration applied, tabs.tsx generated, mermaid in package.json, 2 test files with skipped stubs.
</objective>

<execution_context>
@/Users/Peyton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Peyton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-study-experience/05-CONTEXT.md
@.planning/phases/05-study-experience/05-RESEARCH.md
</context>

<interfaces>
<!-- Key existing contracts the executor needs. -->

From src/Api/Models/Section.cs:
```csharp
public class Section
{
    public Guid Id { get; set; }
    public Guid ModuleId { get; set; }
    public Module Module { get; set; } = null!;
    public int HeadingLevel { get; set; }
    public string HeadingText { get; set; } = string.Empty;
    public string Content { get; set; } = string.Empty;
    public string? SourcePageRefsJson { get; set; }
    public int SortOrder { get; set; }
    public DateTime CreatedAt { get; set; } = DateTime.UtcNow;
}
```

From src/Api/Models/ExtractionRun.cs (existing pattern to mirror):
- Entity has Guid Id, FK + nav property, status/data fields, DateTime UpdatedAt
- AppDbContext has DbSet + OnModelCreating unique index config
- HasConversion<string>() used for enum-like string columns

From src/Api/Data/AppDbContext.cs (existing pattern):
- DbSet<T> as property `=> Set<T>()`
- OnModelCreating configures unique indexes and FK delete behavior
- Existing unique index example: `.HasIndex(r => new { r.ModuleId }).IsUnique()`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: SectionProgress entity, AppDbContext wiring, and EF migration</name>
  <files>src/Api/Models/SectionProgress.cs, src/Api/Data/AppDbContext.cs</files>
  <action>
Create `src/Api/Models/SectionProgress.cs`:
```csharp
namespace StudyApp.Api.Models;

public class SectionProgress
{
    public Guid Id { get; set; }
    public Guid SectionId { get; set; }
    public Section Section { get; set; } = null!;
    public Guid UserId { get; set; }
    public int CardsReviewed { get; set; } = 0;
    public bool QuizCompleted { get; set; } = false;
    public DateTime UpdatedAt { get; set; } = DateTime.UtcNow;
}
```

In `src/Api/Data/AppDbContext.cs`, add:
- `public DbSet<SectionProgress> SectionProgresses => Set<SectionProgress>();`
- In `OnModelCreating`, configure SectionProgress:
```csharp
modelBuilder.Entity<SectionProgress>(entity =>
{
    entity.HasKey(sp => sp.Id);
    entity.HasIndex(sp => new { sp.SectionId, sp.UserId }).IsUnique();
    entity.HasOne(sp => sp.Section)
          .WithMany()
          .HasForeignKey(sp => sp.SectionId)
          .OnDelete(DeleteBehavior.Cascade);
});
```

Then generate and apply the migration from the repo root:
```bash
dotnet ef migrations add AddSectionProgress --project src/Api
dotnet ef database update --project src/Api
```
  </action>
  <verify>
    <automated>dotnet build src/Api && dotnet ef migrations list --project src/Api | grep AddSectionProgress</automated>
  </verify>
  <done>Migration AddSectionProgress exists, `dotnet build src/Api` succeeds with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 test stubs and frontend dependency installation</name>
  <files>src/Api.Tests/Study/StudyEndpointTests.cs, src/Api.Tests/Study/StudyProgressTests.cs</files>
  <action>
Create directory `src/Api.Tests/Study/` and two test stub files.

`src/Api.Tests/Study/StudyEndpointTests.cs`:
```csharp
namespace StudyApp.Api.Tests.Study;

public class StudyEndpointTests
{
    [Fact(Skip = "Wave 0 stub — implement in plan 05-02")]
    public Task GetSections_ReturnsSectionsWithProgress() => Task.CompletedTask;

    [Fact(Skip = "Wave 0 stub — implement in plan 05-02")]
    public Task GetStudyGuide_ReturnsStudyGuideFields() => Task.CompletedTask;

    [Fact(Skip = "Wave 0 stub — implement in plan 05-02")]
    public Task GetFlashcards_ReturnsFlashcardList() => Task.CompletedTask;

    [Fact(Skip = "Wave 0 stub — implement in plan 05-02")]
    public Task GetConceptMap_Returns200_WhenConceptMapExists() => Task.CompletedTask;

    [Fact(Skip = "Wave 0 stub — implement in plan 05-02")]
    public Task GetConceptMap_Returns404_WhenNoConceptMap() => Task.CompletedTask;

    [Fact(Skip = "Wave 0 stub — implement in plan 05-02")]
    public Task GetSections_IncludesCardsReviewedAndQuizCompleted() => Task.CompletedTask;
}
```

`src/Api.Tests/Study/StudyProgressTests.cs`:
```csharp
namespace StudyApp.Api.Tests.Study;

public class StudyProgressTests
{
    [Fact(Skip = "Wave 0 stub — implement in plan 05-02")]
    public Task UpsertProgress_CreatesRow_WhenNoneExists() => Task.CompletedTask;

    [Fact(Skip = "Wave 0 stub — implement in plan 05-02")]
    public Task UpsertProgress_UpdatesRow_WhenRowAlreadyExists() => Task.CompletedTask;
}
```

Then install frontend dependencies (run from repo root):
```bash
cd /Users/Peyton/repos/study-webapp-pa-school/src/Frontend && npm install mermaid
cd /Users/Peyton/repos/study-webapp-pa-school/src/Frontend && npx shadcn add tabs --yes
```
  </action>
  <verify>
    <automated>dotnet test src/Api.Tests --no-build 2>&1 | grep -E "skipped|passed|failed" && ls src/Frontend/src/components/ui/tabs.tsx && grep '"mermaid"' src/Frontend/package.json</automated>
  </verify>
  <done>Test suite compiles and stubs appear as skipped (not failed). tabs.tsx exists. mermaid appears in package.json dependencies.</done>
</task>

</tasks>

<verification>
- `dotnet build src/Api` exits 0
- `dotnet test src/Api.Tests` shows stubs as skipped, all other tests still pass
- `dotnet ef migrations list --project src/Api` includes AddSectionProgress
- `src/Frontend/src/components/ui/tabs.tsx` exists
- `src/Frontend/package.json` contains `"mermaid"`
</verification>

<success_criteria>
SectionProgress entity + unique index migration applied to local DB. Wave 0 test stubs compile as skipped. Frontend has mermaid installed and tabs.tsx generated. Wave 1 can start without any setup work.
</success_criteria>

<output>
After completion, create `.planning/phases/05-study-experience/05-01-SUMMARY.md`
</output>
