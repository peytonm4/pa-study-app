---
phase: 05-study-experience
plan: 05
type: execute
wave: 3
depends_on: [05-03, 05-04]
files_modified: []
autonomous: false
requirements: [STUDY-01, STUDY-02, STUDY-03, STUDY-04, STUDY-05]
must_haves:
  truths:
    - "Student can browse all sections in reference mode without being forced into a flow"
    - "Student can complete a full guided session: preview → flashcards → quiz → completion screen"
    - "Progress badges on section list reflect actual server-side state after session completion"
    - "Concept map tab renders Mermaid SVG correctly in browser (not a blank box)"
    - "All STUDY-01 through STUDY-05 requirements verified by human"
  artifacts:
    - path: "src/Frontend/src/pages/StudyPage.tsx"
      provides: "Section list with progress badges (visually verified)"
    - path: "src/Frontend/src/pages/SectionDetailPage.tsx"
      provides: "Tabbed reference mode (visually verified)"
    - path: "src/Frontend/src/pages/SessionPage.tsx"
      provides: "Guided session step machine (functionally verified)"
  key_links:
    - from: "progress badges on StudyPage"
      to: "SectionProgress server state"
      via: "useQuery on listSections after session completion"
      pattern: "cardsReviewed|quizCompleted"
---

<objective>
Human verification checkpoint: confirm all Phase 5 study experience features work end-to-end in the running app before phase is marked complete.

Purpose: Phase 5 is the user-facing cap of the entire v1.0 milestone. Human verification ensures the study flow is correct, Mermaid renders visually, and progress persists properly before closing out the project.
Output: Phase 5 approved by user; STUDY-01 through STUDY-05 confirmed complete.
</objective>

<execution_context>
@/Users/Peyton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Peyton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-study-experience/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-verification checks</name>
  <files></files>
  <action>
Run the full test suite and confirm it is green:
```bash
dotnet test src/Api.Tests
```

Confirm frontend builds cleanly:
```bash
cd src/Frontend && npm run build
```

Apply any pending migrations if needed:
```bash
dotnet ef database update --project src/Api
```

Report results. If any tests are red, list them. If build fails, list the errors.
  </action>
  <verify>
    <automated>dotnet test src/Api.Tests && cd src/Api.Tests/../Frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Full test suite green. Frontend build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — full study experience end-to-end</name>
  <action>Pause for human to verify the complete Phase 5 study experience in the running app.</action>
  <what-built>
Complete Phase 5 study experience:
- StudyPage (/study/:moduleId): section list with Browse/Study buttons and progress badges
- SectionDetailPage (/study/:moduleId/sections/:sectionId): tabbed reference mode (Study Guide, Flashcards, Quiz, optional Concept Map)
- SessionPage (/study/:moduleId/sections/:sectionId/session): guided session with preview, flashcard flip, multiple choice quiz, completion screen
- ModuleDetailPage: "Browse Study Materials" button when generationStatus=Ready
- SectionProgress backend: persists cardsReviewed and quizCompleted per section+user
  </what-built>
  <how-to-verify>
Start the full stack if not running:
- `docker compose up -d` (Postgres + MinIO)
- `dotnet run --project src/Api` (API on port 5159)
- `dotnet run --project src/Worker` (Worker)
- `cd src/Frontend && npm run dev` (Frontend on port 5173)

Ensure you have a module with generationStatus=Ready (run through Phase 4 flow if needed).

**STUDY-01: Reference mode — Study Guide and navigation**
1. Open a module with Ready generation status
2. Verify "Browse Study Materials" button appears
3. Click it — should navigate to /study/:moduleId section list
4. Verify section list shows section titles
5. Click "Browse" on any section
6. Verify tabbed page opens with "Study Guide" tab active
7. Verify DirectAnswer appears highlighted/bold at top
8. Verify structured sections below (High-Yield Details, etc.) render as lists

**STUDY-02: Reference mode — Flashcards**
9. Click "Flashcards" tab
10. Verify cards show with front text visible
11. Click "Reveal" on a card — back text appears inline
12. Verify other cards remain in collapsed state (independent toggles)

**STUDY-03: Reference mode — Concept Map**
13. If section has a concept map: verify "Concept Map" tab is present and shows a rendered SVG diagram (not a blank box)
14. If section has no concept map: verify "Concept Map" tab is absent entirely

**STUDY-04: Guided session**
15. Go back to section list, click "Study" on any section
16. Verify Step 1 (Preview): study guide content visible, "Start Flashcards →" button at bottom
17. Click "Start Flashcards →" — Step 2 (Flashcards): single card shown
18. Verify "Flip" button reveals back; "Next →" is disabled until flipped
19. Click Flip, then Next — verify card advances to next card (or quiz step if last card)
20. Complete all cards — verify transition to Step 3 (Quiz)
21. Verify one question at a time with multiple choice options
22. Select an answer — verify immediate feedback (green = correct, red = wrong)
23. Click "Next Question →", repeat through all questions
24. Verify Step 4 (Completion): shows "X of Y correct" score + completion message

**STUDY-05: Progress tracking**
25. On completion screen: go back to section list
26. Verify section that was just studied shows progress badges: "X cards reviewed" and "Quiz Done"
27. Re-enter the same session and complete it again — verify cardsReviewed badge updates

Report "approved" if all checks pass, or describe any issues found.
  </how-to-verify>
  <verify>
    <automated>MISSING — visual and functional verification required by human</automated>
  </verify>
  <done>User reports "approved". All STUDY-01 through STUDY-05 confirmed in the live app.</done>
  <resume-signal>Type "approved" to close Phase 5 and trigger phase verification, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human confirms all STUDY-01 through STUDY-05 requirements are met in the live application.
</verification>

<success_criteria>
User reports "approved". All 5 STUDY requirements confirmed working in the running app. Phase 5 study experience complete.
</success_criteria>

<output>
After completion, create `.planning/phases/05-study-experience/05-05-SUMMARY.md`
</output>
