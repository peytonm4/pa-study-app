---
phase: 03-objectives-and-figures
plan: 07
type: execute
wave: 4
depends_on: [03-06]
files_modified: []
autonomous: false
requirements: [FIG-02, FIG-04, LEXT-05, LEXT-06, SKILL-03]

must_haves:
  truths:
    - "Figure thumbnails are visible in the browser after document upload"
    - "Keep/Ignore toggles update the UI immediately without a page reload"
    - "Run Extraction button triggers the job and status transitions Queued -> Processing -> Ready"
    - "Download button appears and clicking it downloads a valid .docx file"
    - "Full pipeline works in stub mode without Python installed (PYTHON_PROVIDER=stub)"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Phase 3 pipeline running end-to-end in stub mode. Claude automates everything — user confirms the visual result and downloads the .docx.

Purpose: Validates the behaviors that require a real browser, running services, and visual inspection — things automated tests cannot cover.
Output: Human confirms Phase 3 is complete, or reports issues for gap closure.
</objective>

<execution_context>
@/Users/Peyton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Peyton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-objectives-and-figures/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and start the stack in stub mode</name>
  <files></files>
  <action>
Run the full automated test suite to confirm zero failures:
```bash
dotnet test /Users/Peyton/repos/study-webapp-pa-school/src/Api.Tests --no-build
```

If any tests fail, stop and report. Do NOT proceed to the checkpoint if tests are red.

Then confirm the stack is running (or start it):
```bash
cd /Users/Peyton/repos/study-webapp-pa-school && docker compose up -d
dotnet run --project src/Api &
dotnet run --project src/Worker &
cd src/Frontend && npm run dev &
```

Verify all three are up:
- API: curl http://localhost:5159/health
- Worker: check docker compose logs worker (no fatal errors)
- Frontend: http://localhost:5173

Ensure environment variables are set for stub mode:
- PYTHON_PROVIDER=stub (default — no change needed unless explicitly set to "real")
- VISION_PROVIDER=stub (existing default)
- GENERATION_PROVIDER=stub (existing default)
  </action>
  <verify>
    <automated>dotnet test /Users/Peyton/repos/study-webapp-pa-school/src/Api.Tests --no-build 2>&1 | tail -5</automated>
  </verify>
  <done>dotnet test exits 0. API responds to /health. Frontend loads at localhost:5173.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 3 pipeline: figure extraction, figure review UI, lecture extraction job, .docx generation and download. All running in stub mode (no Python, no LLM API keys required).
  </what-built>
  <how-to-verify>
    1. Open http://localhost:5173 in a browser
    2. Navigate to an existing module (or upload a PPTX — use any .pptx file)
    3. Wait for document ingestion to complete (status shows Ready)
    4. Scroll down to the "Figures Review" section — confirm figure cards appear (stub mode shows 2 stub figures)
    5. Verify one figure shows "Figure" label and appears pre-selected (Keep state)
    6. Click the Keep/Ignore toggle on a figure — confirm it toggles without a page reload
    7. Scroll to the "Lecture Extraction" section — confirm "Run Extraction" button is present
    8. Click "Run Extraction" — confirm status badge transitions: Queued → Processing → Ready (may be fast in stub mode)
    9. When status shows Ready, confirm "Download Lecture (.docx)" button appears
    10. Click the download button — confirm a .docx file is downloaded
    11. Open the downloaded .docx in Word or Google Docs — confirm it contains headings "Overview" and "Key Concepts" (stub content)

    Expected in stub mode:
    - Figure thumbnails may show broken images (S3 keys are stub paths) — this is acceptable
    - Status transitions quickly because StubSkillRunner returns immediately
    - .docx contains stub headings and content
  </how-to-verify>
  <resume-signal>Type "approved" if all steps pass, or describe which step failed and what you saw.</resume-signal>
</task>

</tasks>

<verification>
Human confirms the end-to-end pipeline works in stub mode.
</verification>

<success_criteria>
- dotnet test exits 0 (all automated tests pass)
- Figure review section renders in the browser
- Keep/Ignore toggles work
- Extraction job runs in stub mode and transitions to Ready
- Download button appears and .docx downloads
- Human types "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/03-objectives-and-figures/03-07-SUMMARY.md`
</output>
